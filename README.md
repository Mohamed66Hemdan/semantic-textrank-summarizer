# semantic-textrank-summarizer
An extractive text summarization application built with Sentence Transformers and TextRank. It uses semantic embeddings, cosine similarity, and PageRank to identify the most important sentences. The project includes an interactive Streamlit interface for generating concise summaries.
